import { Loading, Notification, MessageBox, Message } from 'element-ui';
import UMEMessage from './UMEMessage';

const UI = {
  Loading,
  Notification,
  MessageBox,
  Message,
  UMEMessage,
};

export default UI;
